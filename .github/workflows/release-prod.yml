name: "Release to PROD"

on:
  release:
    types: [published]
  workflow_dispatch: {}

jobs:
  release-prod:
    name: "ODCS Quality Gate — PROD Release"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Release to PROD
        uses: ./.github/actions/ontos-dqx
        with:
          environment: prod
          databricks-host: ${{ secrets.DATABRICKS_HOST }}
          databricks-token: ${{ secrets.DATABRICKS_TOKEN }}
          warehouse-id: ${{ secrets.DATABRICKS_WAREHOUSE_ID }}
          ontos-base-url: ${{ secrets.ONTOS_BASE_URL }}
          sp-client-id: ${{ secrets.SP_CLIENT_ID }}
          sp-client-secret: ${{ secrets.SP_CLIENT_SECRET }}
